## 📂 Folder Structure

```text
src/
├── app/            # Rutas y páginas (layout, cart, products)
├── components/     # Componentes reutilizables
│   ├── ui/         # Componentes atómicos (Shadcn UI)
│   └── common/     # Componentes globales (Header, Footer)
├── constants/      # Datos estáticos (Mockups de productos)
├── hooks/          # Lógica personalizada (useCart, useAuth)
├── store/          # Gestión de estado (Zustand)
└── types/          # Definiciones de TypeScript